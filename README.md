# DSTR_Assignment

Data Structures assignment — Task 1.

## Repository Structure

```text
/
├── documents/       # Assignment briefs, marking schemes, and reference PDFs
├── datasets/        # Input CSV datasets used by the program
└── src/             # Source code (add your code here)
```

## Branches

- `main` — stable, reviewed code only. Merge here when a feature is complete and tested.
- `development` — active development. Branch off here for new features or fixes, then merge back via pull request.

## Workflow

1. Clone the repo and switch to `development`.
2. Create a feature branch from `development` (e.g. `feature/sorting-algorithm`).
3. Commit your changes with clear messages.
4. Open a pull request into `development` when ready for review.
5. `main` is updated from `development` once a milestone is stable.

## Datasets

| File | Description |
| ---- | ----------- |
| `datasets/dataset1_city_a.csv` | City A traffic/event data |
| `datasets/dataset2_city_b.csv` | City B traffic/event data |
| `datasets/dataset3_city_c.csv` | City C traffic/event data |
